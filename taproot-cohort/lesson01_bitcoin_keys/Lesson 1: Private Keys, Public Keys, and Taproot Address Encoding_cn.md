# 第 1 课：私钥、公钥和 Taproot 地址编码

**周次**: 1  
**时长**: 自主学习（预计 3-4 小时）  
**先修要求**: 基本的密码学和比特币交易理解

---

## 🎯 学习目标

完成本课后，你将能够：

1. 生成比特币私钥并理解其密码学特性
2. 使用椭圆曲线密码学从私钥派生公钥
3. 创建多种格式的地址（Legacy、SegWit、Taproot）
4. 理解 WIF、私钥和地址编码之间的关系
5. 解释 x-only 公钥在 Taproot 中的重要性
6. 构建和解码 Bech32m 编码的 Taproot 地址

---

## 🧩 核心概念

### 密码学层次结构

比特币的安全模型依赖于**单向**数学关系：

```
私钥 (256-bit) → 公钥 (ECDSA 点) → 地址 (编码哈希)
```

**关键安全属性：**
- **正向派生**：容易（私钥 → 公钥 → 地址）
- **反向派生**：计算不可行（无法向后推导）
- **抗碰撞性**：不同密钥产生相同地址的概率极低

### 私钥：所有权的基础

比特币私钥是一个**256 位随机数**：
- **密钥空间**：2^256 种可能的密钥（比宇宙中的原子数还多）
- **熵要求**：必须是真正的随机数，不可预测
- **表示格式**：
  - 十六进制（64 个字符，例如 `e9873d...`）
  - WIF - 钱包导入格式（Base58Check 编码）

**WIF 编码过程：**
```
1. 添加版本前缀（0x80 主网，0xEF 测试网）
2. 可选：为压缩公钥添加 0x01
3. 计算校验和：SHA256(SHA256(data))，取前 4 字节
4. 应用 Base58 编码
```

**WIF 前缀：**
- `L` 或 `K`：主网私钥
- `c`：测试网私钥

### 公钥：密码学验证点

公钥是**secp256k1 椭圆曲线上的点**：

**曲线方程**：`y² = x³ + 7`

**格式选项：**

| 格式 | 大小 | 结构 | 使用场景 |
|------|------|------|----------|
| **未压缩** | 65 字节 | `04 + x (32) + y (32)` | 传统格式，很少使用 |
| **压缩** | 33 字节 | `02/03 + x (32)` | 现代标准 |
| **X-only (Taproot)** | 32 字节 | `仅 x 坐标` | Taproot 专用 |

**压缩机制：**
- 曲线的数学性质允许从 x 重建 y
- 前缀 `02`：y 是偶数
- 前缀 `03`：y 是奇数

**Taproot 的 X-only 创新：**
- 仅使用 x 坐标（32 字节）
- 减少交易大小
- 支持密钥聚合和 Schnorr 签名
- 简化签名验证

### 地址生成：用户友好的支付目的地

地址**不是**公钥——它们是公钥的**编码哈希**。

**为什么使用地址？**
- **隐私性**：在花费之前不会直接暴露公钥
- **抗量子性**：哈希函数提供后量子安全层
- **错误检测**：内置校验和防止输入错误

**地址生成流程：**
```
1. 哈希公钥：SHA256(pubkey) → RIPEMD160 = Hash160
2. 添加元数据：版本字节 + 脚本类型信息
3. 添加校验和：错误检测
4. 编码：Base58Check 或 Bech32(m)
```

### 地址类型对比

| 类型 | 编码 | 前缀 | 示例 | 主要用途 |
|------|------|------|------|----------|
| **P2PKH** | Base58Check | `1...` | `1A1zP1eP5Q...` | 传统支付 |
| **P2SH** | Base58Check | `3...` | `3J98t1WpEZ...` | 多重签名，包装的 SegWit |
| **P2WPKH** | Bech32 | `bc1q...` | `bc1qw508d6q...` | 原生 SegWit |
| **P2TR** | Bech32m | `bc1p...` | `bc1plz0h3rl...` | Taproot |

**重要洞察：**
> 从节点的角度来看，比特币从不存储地址——只存储脚本（scriptPubKey）。地址是锁定脚本的人类友好表示。一旦你识别出前缀，你就知道它背后的脚本类型。

---

## 💻 示例代码

### 设置：安装所需库

```bash
# 安装所需的 Python 包
pip install python-bitcoinlib bitcoinutils ecdsa
```

### 示例 1：生成密钥和地址

```python
from bitcoinlib.keys import Key

# 生成新的比特币密钥对
key = Key()

# 提取不同格式的私钥
private_key_hex = key.private_hex      # 十六进制格式的 32 字节
private_key_wif = key.wif()            # 钱包导入格式

print("="*60)
print("私钥格式")
print("="*60)
print(f"十六进制: {private_key_hex}")
print(f"WIF: {private_key_wif}")
print()

# 生成不同格式的公钥
public_key_compressed = key.public_hex                    # 33 字节
public_key_uncompressed = key.public_uncompressed_hex     # 65 字节

print("="*60)
print("公钥格式")
print("="*60)
print(f"压缩 (33 字节):   {public_key_compressed}")
print(f"未压缩 (65 字节): {public_key_uncompressed[:70]}...")
print()

# Taproot x-only 公钥 (32 字节)
taproot_pubkey = key.public_hex[2:]  # 移除 02/03 前缀

print("="*60)
print("TAPROOT X-ONLY 公钥")
print("="*60)
print(f"X-only (32 字节): {taproot_pubkey}")
print()

# 从同一个密钥生成不同地址类型
legacy_address = key.address()                                     # P2PKH
segwit_native = key.address(encoding='bech32')                     # P2WPKH
segwit_p2sh = key.address(encoding='base58', script_type='p2sh')   # P2SH-P2WPKH
taproot_address = key.address(script_type='p2tr')                  # P2TR

print("="*60)
print("地址格式（全部来自同一个私钥）")
print("="*60)
print(f"传统 (P2PKH):    {legacy_address}")
print(f"SegWit 原生:     {segwit_native}")
print(f"SegWit P2SH:     {segwit_p2sh}")
print(f"Taproot:         {taproot_address}")
print()
```

**预期输出结构：**
```
============================================================
私钥格式
============================================================
十六进制: e9873d79c6d87dc0fb6a5778633389dfa5c32fa27f99b5199abf2f9848ee0289
WIF: L1aW4aubDFB7yfras2S1mN3bqg9w3KmCPSM3Qh4rQG9E1e84n5Bd

============================================================
公钥格式
============================================================
压缩 (33 字节):   0250be5fc44ec580c387bf45df275aaa8b27e2d7716af31f10eeed357d126bb4d3
未压缩 (65 字节): 0450be5fc44ec580c387bf45df275aaa8b27e2d7716af31f10eeed357d...

============================================================
TAPROOT X-ONLY 公钥
============================================================
X-only (32 字节): 50be5fc44ec580c387bf45df275aaa8b27e2d7716af31f10eeed357d126bb4d3

============================================================
地址格式（全部来自同一个私钥）
============================================================
传统 (P2PKH):    1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa
SegWit 原生:     bc1qw508d6qejxtdg4y5r3zarvary0c5xw7kygt080
SegWit P2SH:     3J98t1WpEZ73CNmQviecrnyiWrnqRhWNLy
Taproot:          bc1plz0h3rlj2zvn88pgywqtr9k3df3p75p3ltuxh0
```

### 示例 2：导入现有密钥并验证派生

```python
from bitcoinlib.keys import Key

# 导入一个测试网私钥（仅用于教育目的）
testnet_wif = 'cPeon9fBsW2BxwJTALj3hGzh9vm8C52Uqsce7MzXGS1iFJkPF4AT'

# 从 WIF 创建密钥对象
key = Key(import_key=testnet_wif, network='testnet')

print("="*60)
print("导入的测试网密钥验证")
print("="*60)
print(f"网络: {key.network.name}")
print(f"私钥 (十六进制): {key.private_hex}")
print(f"私钥 (WIF): {key.wif()}")
print(f"公钥: {key.public_hex}")
print(f"传统地址: {key.address()}")
print(f"Taproot 地址: {key.address(script_type='p2tr')}")
```

### 示例 3：理解地址前缀识别

```python
def identify_address_type(address):
    """
    根据前缀识别比特币地址类型
    """
    if address.startswith('1'):
        return "P2PKH (传统支付到公钥哈希)"
    elif address.startswith('3'):
        return "P2SH (支付到脚本哈希)"
    elif address.startswith('bc1q'):
        return "P2WPKH (原生 SegWit)"
    elif address.startswith('bc1p'):
        return "P2TR (Taproot)"
    elif address.startswith('tb1q'):
        return "P2WPKH (测试网原生 SegWit)"
    elif address.startswith('tb1p'):
        return "P2TR (测试网 Taproot)"
    else:
        return "未知或非标准格式"

# 测试函数
addresses = [
    "1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa",
    "3J98t1WpEZ73CNmQviecrnyiWrnqRhWNLy",
    "bc1qw508d6qejxtdg4y5r3zarvary0c5xw7kygt080",
    "bc1plz0h3rlj2zvn88pgywqtr9k3df3p75p3ltuxh0",
]

print("="*60)
print("地址类型识别")
print("="*60)
for addr in addresses:
    addr_type = identify_address_type(addr)
    print(f"{addr[:20]}... → {addr_type}")
```

---

## 🧪 实践任务

### 任务 1：生成你的第一个测试网 Taproot 地址

**目标**：创建完整的密钥派生流程并在比特币测试网上测试。

**步骤：**

1. **生成新的密钥对**，使用上面的代码
2. **提取所有格式**：
   - 私钥（十六进制和 WIF）
   - 公钥（压缩和 x-only）
   - 所有地址类型
3. **保存你的 Taproot 测试网地址**以供未来课程使用
4. **获取测试网币**：访问 https://coinfaucet.eu/en/btc-testnet/ 并向你的地址发送一些币
5. **验证接收**：在测试网浏览器上检查你的地址（例如，https://mempool.space/testnet）

**交付成果**：
- 截屏你的地址接收测试网币的情况
- 将你的私钥 WIF 保存在安全笔记中（未来课程需要）

### 任务 2：地址派生验证

**挑战**：给定这个私钥（仅测试网，教育用途）：
```
cPeon9fBsW2BxwJTALj3hGzh9vm8C52Uqsce7MzXGS1iFJkPF4AT
```

编写代码：
1. 导入私钥
2. 派生公钥
3. 生成 Taproot 地址
4. 验证地址以 `tb1p` 开头（测试网 Taproot 前缀）

**预期结果**：
你的代码应该生成一个测试网 Taproot 地址。验证它与你手动派生的地址是否匹配。

### 任务 3：编码侦探工作

**问题**：为什么 Taproot 使用 Bech32m 而不是 Bech32？

研究 BIP 350，并用 2-3 句话解释 Bech32 中 Bech32m 修复的特定缺陷。

---

## 📘 阅读材料

### 必读材料
- **Mastering Taproot** - 第 1 章：私钥、公钥和地址编码（第 1-11 页）

### 补充资源
- **BIP 340**：secp256k1 的 Schnorr 签名 - https://github.com/bitcoin/bips/blob/master/bip-0340.mediawiki
- **BIP 341**：Taproot：SegWit 版本 1 支出规则 - https://github.com/bitcoin/bips/blob/master/bip-0341.mediawiki
- **BIP 350**：v1+ 见证地址的 Bech32m 格式 - https://github.com/bitcoin/bips/blob/master/bip-0350.mediawiki
- **Bitcoin Optech**：Taproot 工作坊 - https://bitcoinops.org/en/schorr-taproot-workshop/

### 推荐深入学习
- 椭圆曲线密码学可视化解释：https://www.youtube.com/watch?v=NF1pwjL9-DE
- Base58Check 编码规范
- 测试网水龙头和浏览器工具

---

## 💬 讨论话题

在 Discord #week-1-discussion 频道发布你的想法：

### 问题 1：隐私影响
**"Taproot 中的 x-only 公钥格式与 SegWit 中的压缩公钥相比如何改善隐私？消除了哪些信息，这对 Taproot 的密钥聚合功能为何重要？"**

*提示：思考 02/03 前缀揭示的信息，以及 Schnorr 签名如何以不同方式处理这一点。*

### 问题 2：抗量子性
**"比特币地址使用 Hash160（SHA256 + RIPEMD160）在将公钥编码为地址之前对其进行哈希。这如何提供一层后量子保护？当你从地址花费时，这种保护会发生什么？"**

*提示：考虑在第一次花费之前和之后链上揭示的信息。*

### 问题 3：编码权衡
**"Taproot 地址比 SegWit 地址更长（58-62 个字符 vs 42-46 个字符）。鉴于 Taproot 应该更高效，为什么我们接受这种大小增加？Bech32m 编码提供了哪些好处来证明额外长度的合理性？"**

---

## 🎙️ 讲师笔记

欢迎来到 Taproot 脚本工程队列！这第一课建立了我们在整个课程中将构建的密码学基础。

你可能对不同的格式和编码感到困惑——这很正常。要记住的关键洞察是：**地址是为人类准备的，脚本是为节点准备的**。我们学习的关于密钥格式和地址编码的一切，都是为理解 Taproot 如何让多个支出条件隐藏在单一、统一的地址格式后面做准备。

在这个队列结束时，你将能够熟练地构建多叶子 Taproot 树，其中每个叶子代表不同的支出路径——同时保持未使用路径的隐私。但首先，我们需要掌握基础知识。

请认真对待实践任务，特别是获取测试网币。在接下来的课程中，你将需要这些来进行动手交易构建。

在 Discord 上见，进行问答！

—Aaron

---

## 📌 关键要点

✅ 私钥（256 位）是所有权的最终来源  
✅ 公钥通过椭圆曲线乘法派生（单向函数）  
✅ X-only 公钥（32 字节）是 Taproot 的效率创新  
✅ 地址是公钥的编码哈希，而不是公钥本身  
✅ Bech32m 编码（bc1p...）是 Taproot 地址的标准  
✅ 理解派生层次结构对 Taproot 开发至关重要

---

**下一课**：[第 2 课：栈操作和 P2PKH 执行 →](./lesson-02-p2pkh-stack-execution.md)

---

*本课程是 Chaincode Labs (2026) Taproot 脚本工程队列的一部分*

